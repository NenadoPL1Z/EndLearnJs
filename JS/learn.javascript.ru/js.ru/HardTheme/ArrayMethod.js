//let arr = [1, 2, 3, 4, 5, 6, 7, 10, 11, 12, 13, 14];
//arr.splice(0, 1, "Один") // удаляем 1 элемент с индекса 0 и заменяем на Один
//// console.log(arr)
//// console.log(arr.slice(1, 10)) // возвращаем все элементы с 1 - 10(не включая)
//
//// let x = []
//// console.log(x.concat(arr, 15, 16)) // копируем данные и добовляем в 1 массив
//
//arr.forEach(function(item, index, array){ // тоже самое что и map
// (`Цифра ${item}, с индексом ${index} в массиве ${array}`)
//});
//let map = arr.map(item => (item + "10"))
//
//console.log(arr.indexOf(12, 0)) // ищем элемент 12 в массиве начиная с 0 индекса
//
//let x = [10, 20, 30, 40, 50, 60, 70, 80, 90]
//
//let reduce = x.reduce(function(progress, item){ // проходим по каждому элементу с переносом результата
//  return progress + item
//});
//console.log(reduce)
